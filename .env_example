# AI Configuration
GEMINI_API_KEY=your_gemini_api_key_here

# Backend Configuration
APP_NAME="RPG FastAPI Backend"

# Database Configuration (Defaults to local SQLite if omitted)
DATABASE_URL=sqlite+aiosqlite:///./rpg.db

# Any Frontend Environment Variables (Prefix with VITE_ for Vite projects if needed)
# VITE_API_BASE_URL=http://localhost:8000
